// #codes
const codes = document.getElementById('codes');

const coded = ()=>{
       const codes = document.getElementById('codes');
      
 const codes_v = codes.value;
 if (codes_v == "1234"){
      alert('The number you have enteed is ${codes_v}. This is what you can do now')
      }
}
